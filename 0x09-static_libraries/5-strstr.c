#include "main.h"
#include <string.h>

/**
 * _strstr - locates a substring.
 * @needle: first occurence of substring.
 * @haystack: string.
 * Return:  a poiter, or NULL if the substring is not found.
 */

char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
	return (0);
}
