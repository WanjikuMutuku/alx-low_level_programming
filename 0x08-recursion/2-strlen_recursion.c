#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - returns the length of a string.
 * @s: string whereby length is determined.
 *
 * Return: Always 0.
 */

int _strlen_recursion(char *s)
{
	return (strlen(s));

	return (0);
}
