#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	int y;

	for (y = 0; y <= 10; y++)
	{
		putchar((num % 10) + '0');
	}
	putchar('\n');
	return (0);
}
