#include "main.h"

/**
 * _atoi - function that convert a string to an integer.
 * @s: value to be converted to integer.
 *
 * Return: Always 0.
 */

int _atoi(char *s)
{
	int val;
	val = _atoi(s);
	return(0);
}
